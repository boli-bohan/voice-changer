apiVersion: v1
kind: Service
metadata:
  name: {{ include "voice-changer.frontend.fullname" . }}
  labels:
    {{- include "voice-changer.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
  - port: {{ .Values.frontend.service.port }}
    targetPort: {{ .Values.frontend.service.targetPort }}
    {{- if eq .Values.frontend.service.type "NodePort" }}
    nodePort: {{ .Values.frontend.service.nodePort }}
    {{- end }}
    protocol: TCP
    name: http
  selector:
    {{- include "voice-changer.frontend.selectorLabels" . | nindent 4 }}
